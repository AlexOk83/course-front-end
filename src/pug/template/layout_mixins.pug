mixin menu(menu, page, parent_class)
	div(class= parent_class + '__menu')
		each item in menu
			if (page == item.url)
				li(class= parent_class + '__menu-item ' + parent_class + '__menu-item--active')
					a(href= item.url + '.html')= item.title
			else
				li(class= parent_class + '__menu-item')
					a(href= item.url + '.html')= item.title
mixin header(page)
	header.header(class={'.header--main': page == 'index'})
		.container
			a.header__logo(href="index.html")= logo
			+menu(menu, page, 'header')
			.header__contacts
				.header__contacts-button.form-control__button I Want To Avoid Surgery
				.header__contacts-phone= phone
			.header__device-cont
				.header__device-phone
				.header__toggle-button: span.header__toggle-button-lines

mixin device_menu(page)
	.device-toggle-content
		.container
			+menu(menu, page, 'device-toggle-content')
			.device-toggle-content__button I Want To Avoid Surgery
			+social_icons()

mixin footer()
	footer.footer

mixin toggleMenu()
	.header__toggle-menu

mixin social_icons()
	.social-icons
		a.icon.icon--linkdIn(href='#')
		a.icon.icon--google(href='#')
		a.icon.icon--facebook(href='#')
		a.icon.icon--twitter(href='#')

mixin footer(page)
	footer.footer
		.container
			.footer__row
				.footer__menu-icons
					+menu(menu, page, 'footer')
					+social_icons()
				.footer__phone-address-email
					p.footer__phone= phone
					p.footer__email boulderprolo@earthlink.net
					p.footer__addres 1790 30th Street, Suite 230, Boulder, CO
				.footer__copy-raiting-desing
					.footer__copy-raiting
						p.footer__copy © 2018 Boulder Prolotherapy. All Rights Reserved
						.footer__rating
							p.footer__rating-text Company rating:
							+rating('four')
					p.footer__design Web Design — Direct Line Development


